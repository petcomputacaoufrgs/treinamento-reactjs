class APIConstants {
  private URL_ROOT = 'http://treinamento-reactjs-api.herokuapp.com/'
  GET_SVG_ALL = this.URL_ROOT + 'svg/all'
  GET_SVG_BY_IDS = this.URL_ROOT + 'svg/list'
  GET_SVG_BY_QUANTITY = this.URL_ROOT + 'svg/quantity'
}

export default new APIConstants()